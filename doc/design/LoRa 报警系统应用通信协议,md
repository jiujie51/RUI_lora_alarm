# LoRa 报警系统应用通信协议
V1.4

---

## 版本历史
| 版本 | 日期       | 修订人 | 修改内容                                   |
|------|------------|--------|--------------------------------------------|
| 1.0  | 2026-5-6   | Mr G   | 初版                                       |
| 1.1  | 2026-5-11  | Mr G   | 增加 SRP 相关支持                          |
| 1.2  |            |        | 整合收到的反馈                             |
| 1.3  | 2026-5-19  | Mr G   | 修改 CMD 0x02，增加经纬度字段               |
| 1.4  | 2026-5-21  | Mr G   | 修改 Code 与分组 ID 相关协议                |

---

### 标注说明
- 蓝色：新增
- 紫色：修改
- 灰色：未实现

---

## 目录
1. 通信接口
   1.1 上下行端口说明
   1.2 其他特性
2. 指令详情
   2.1 指令格式
   2.3 指令明细
3. CRC16

---

# 1 通信接口
## 1.1 上下行端口说明
本协议用于胸牌（Badge）、节点（Hub）与 LNS 之间通信。

## 1.2 其他特性

---

# 2 指令详情
## 2.1 指令格式
| 帧头   | 版本 | 控制位 | 指令ID | 长度   | CRC16  | 数据   |
|--------|------|--------|--------|--------|--------|--------|
| 2 字节 | 1 字节 | 1 字节 | 1 字节 | 2 字节 | 2 字节 | N 字节 |
| 0xAA55 | X    | X      | X      | XX     | XX     | X…     |

### 帧头（head）
长度：2 字节，固定为 **0xAA55**

### 版本（ver）
长度：1 字节，默认 1

### 指令ID（CMDID）
| CMDID | 指令名称        | 指令说明                                                                 |
|-------|-----------------|--------------------------------------------------------------------------|
| 0x00  | 心跳包          | 网关与 Hub 每 5 分钟上报一次心跳                                         |
| 0x01  | 电量上报        | Badge/Hub 电量变化且间隔超过 5 分钟时上报                                 |
| 0x02  | 按键事件        | 按键触发后上报按键事件                                                   |
| 0x03  | 事件码设置      | 设置运行码（红、蓝、黄、绿、Hold、Secure、Evacuate、Shelter）             |
| 0x04  | 事件码参数配置  | 配置事件码的显示内容                                                     |
| 0x05  | LED 控制        | 控制 LED 灯                                                              |
| 0x06  | 蜂鸣器控制      | 控制蜂鸣器                                                               |
| 0x07  | 震动控制        | 控制震动马达                                                             |
| 0x08  | LCD 内容控制    | 第一行、第二行内容控制                                                   |
| 0x09  | LCD 第二行开关  | 是否显示第二行文字                                                       |
| 0x0A  | 清除包          | 清除全部状态或全部解除                                                   |
| 0x50  | 设置分组ID      | 设置设备分组ID                                                           |

#### 批注
- 记得加上绿色。
- 此代码指令用于服务器下行通信。代码颜色（红色、蓝色、黄色）已在您文档
  “Firmware Requirements & APIs V7 - SIE 更新 - 2026 年 2 月.xlsx”中进行了设定。
  “Code Green”存在吗？
- 可能在要求说明中遗漏了“绿色”这一要素，实际上它是设计的一部分。因为徽章上有一个绿色按钮，
  用于指示某个房间“已解除警报”，并且仅在学校的“红色警报”状态激活期间才会使用该按钮。
  当工作人员按下绿色按钮时，房间外的控制中心会闪烁绿色灯光，同时仪表板会显示该房间已安全。
  请参考其他共享文档，因为它们包含了完整的使用案例。
- CMDID 0x03 标记为“Code”表示“警报”。这是一个下行指令的 CMDID。
  因此，似乎不存在 Code 绿色下行指令的情况。
  如果学校管理员在 Code 红色警报后需要发送“一切安全”的消息，他应该发送 CMDID 0x0A（解除警报包）。
  下行意味着指令来自服务器。
- 正如所解释的那样，“全部解除警报”和“清除所有状态”是来自服务器的下行指令，而固件需要理解这一点。
  然而，佩戴式设备上的绿色按钮在被按下时会为该房间触发“全部解除警报”状态，
  该状态会在佩戴式设备和与该房间相关的门控器上闪烁绿色光。因此，我们需要像定义红色、蓝色和黄色按钮那样来定义这个按钮。
  请注意，某些学校可能希望此时房间门控器也闪烁绿色光……我们在固件层面如何处理这个问题？是通过分组的方式吗？

### 控制位（control）
长度：1 字节

| Bit7 | Bit6 | Bit5 | Bit4 | Bit3   | Bit2~0      |
|------|------|------|------|--------|-------------|
| 保留 | 保留 | 保留 | 保留 | 重发标志 | 包类型      |

- 包类型（PACKET_TYPE）：0=请求包；其他保留
- 重发（RESEND）：0
- 保留位：0

### 长度（length）
包含所有头部信息与数据域的总长度

### CRC16
对头部（不含 CRC16 字段）与数据域使用 **CRC16/XMODEM** 算法计算

### 数据域（Data Field）
详见各指令码格式

---

## 2.3 指令明细
CMDID 0x01~0x02 为上行  
其他 CMDID 为下行

### 2.3.1 指令列表
#### 2.3.1.1 心跳包（0x00）
方向：Badge/Hub → 网关  
说明：设备每 5 分钟上报心跳包

| 字段       | 长度（字节） | 取值    | 说明                                                                 |
|------------|---------------|---------|----------------------------------------------------------------------|
| 设备类型   | 1             | 0/1     | 0=Badge；1=Hub                                                       |
| 分组ID     | 1             | 0~255   | 默认 0（无角色），由 0x50 设置<br/>Bit0=管理员；Bit1=护士；Bit2=安保；Bit3=校长；Bit7=保留 |

示例：  
AA55 01 00 01 0A00 xx xx 01

---

#### 2.3.1.2 电量上报（0x01）
方向：Badge/Hub → 网关  
说明：电量变化上报，间隔不低于 5 分钟

| 字段       | 长度（字节） | 取值    | 说明                     |
|------------|---------------|---------|--------------------------|
| 设备类型   | 1             | 0/1     | 0=Badge；1=Hub           |
| 电量       | 1             | 0~100   | 电量百分比               |

---

#### 2.3.1.3 按键事件（0x02）
方向：Badge/Hub → 网关  
说明：按键触发时上报

| 字段      | 长度（字节） | 取值                                   | 说明                                                                 |
|-----------|---------------|----------------------------------------|----------------------------------------------------------------------|
| 按键      | 1             | 0~3                                    | 0=绿；1=蓝；2=黄；3=红                                               |
| 动作      | 1             | 0/1                                    | 0=短按；1=长按                                                       |
| RSSI      | 1             | 0~255                                  | 蓝牙信号强度，-90dBm 记为 90                                         |
| Hub MAC   | 6             | XX:XX:XX:XX:XX:XX                      | Hub 地址                                                             |
| 纬度      | 4             | Bit31=0北纬/1南纬；Bit0\ ~30=0~90000001  | 90000000=90.000000°；90000001=无效                                   |
| 经度      | 4             | Bit31=0东经/1西经；Bit0\~30=0~180000001 | 180000000=180.000000°；180000001=无效                               |

---

#### 2.3.1.4 事件码（0x03）
方向：网关 → Badge/Hub  
说明：云端下发通知

| 字段     | 长度（字节） | 取值    | 说明                                                                 |
|----------|---------------|---------|----------------------------------------------------------------------|
| 分组ID   | 1             | 0~255   | 目标角色<br/>Bit0=管理员；Bit1=护士；Bit2=安保；Bit3=校长；Bit7=全部 |
| 警报类型 | 1             | 1~7     | 0绿/1蓝/2黄/3红/4~7对应Hold/Secure/Evacuate/Shelter                  |

---

#### 2.3.1.5 事件码配置（0x04）
方向：网关 → Badge/Hub

| 字段                 | 长度（字节） | 取值          | 说明                                                                 |
|----------------------|---------------|---------------|----------------------------------------------------------------------|
| 分组ID               | 1             | 0~255         | 目标角色                                                             |
| 警报类型             | 1             | 0~7           | 警报类型                                                             |
| 使能开关             | 1             | 0x00~0xFF     | Bit0=LCD第二行；Bit1=蜂鸣器；Bit2=震动                               |
| 模式开关             | 1             | 0x00~0xFF     | Bit0=蜂鸣模式(0常响/1闪烁)；Bit1=震动模式                            |
| 音量                 | 1             | 0~10          | 0最小，10最大                                                        |
| 蜂鸣开启时长         | 4             | 0~2147483647  | 单位 ms                                                              |
| 蜂鸣关闭时长         | 4             | 0~2147483647  | 单位 ms                                                              |
| 震动开启时长         | 4             | 0~2147483647  | 单位 ms                                                              |
| 震动关闭时长         | 4             | 0~2147483647  | 单位 ms                                                              |
| LED_R                | 1             | 0~255         |                                                                      |
| LED_G                | 1             | 0~255         |                                                                      |
| LED_B                | 1             | 0~255         |                                                                      |

##### 批注
- 这些颜色对应的 RGB 数值是在哪里定义的？请参考第14页，里面有之前发送过的建议数值。
- 本次警报信号分别代表“红色/蓝色/黄色/暂停/安全/疏散/避难”。
  此命令用于设定设备的移动方式。例如，我们为“红色代码”设定相关参数。
  当服务器向一个标签发送命令 ID 为 0x03 的指令时，该标签将按照命令 ID 为 0x04 的设置来运行指示灯和振动功能。
- 明白了。然而，我们仍需要这些颜色采用 RGB 值表示。您能否确认这些值是在哪里设置/定义的呢？
  例如：红色 - 255, 0, 0，黄色 - 255， 220， 0。所有 RGB 值请见第 14 页。
  同时也要记住以下几点，并将其应用到设计中：
  1. 红色、黄色、蓝色和绿色可以通过徽章上的按钮来触发。
  2. 全校范围的“安全无虞”和“所有状态正常”信息会从仪表板推送出来。
  3. 所有的 SRP 警报（“暂停 - 紫色”/“安全 - 蓝色”/“疏散 - 绿色”/“避难 - 橙色”）
   都会从仪表板推送出来，并传送到所有中心的标识牌上。
- 1. 关于颜色方面，我们将依照第 14 页上的说明来进行。
  1. 关于这个按钮的移动情况，最好能给我们提供一份详细描述的文档。
  2. 关于“一切正常”和“清除所有”，我知道这是由服务器发出的。并且它应该通过 CMDID 0x0A（清除数据包）发送至设备。
  3. 关于 SRP，我知道它是由服务器发送的。所以，它应该通过 CMDID 0x03（代码）发送到设备上。
  并且组 ID 应设置为 255。因为当命令的组 ID 为 255 时，设备会执行该命令而无需匹配定位组 ID。
- 谢谢你！您所说的“按钮的动作”是什么意思？我们能否通过电话进行讨论？
  请注意，“全部清除”和“清除所有状态”是两种不同的操作。前者会将所有设备设置为显示绿色（即“全部清除”状态），
  而后者则会清除所有状态，使设备恢复正常。因此，一个“清除数据包”命令不能同时用于这两种操作。
---

#### 2.3.1.6 LED 控制（0x05）
方向：网关 → Badge/Hub

| 字段                 | 长度（字节） | 取值          | 说明                                                                 |
|----------------------|---------------|---------------|----------------------------------------------------------------------|
| 分组ID               | 1             | 0~255         | 目标角色                                                             |
| 颜色                 | 1             | 0~3           | 颜色类型                                                             |
| 模式开关             | 1             | 0x00~0xFF     | Bit0=LED模式(0常亮/1闪烁)                                            |
| 开关                 | 1             | 0/1           | 模式0：0关1开；模式1忽略                                             |
| LED 开启时长         | 4             | 0~2147483647  | 单位 ms                                                              |
| LED 关闭时长         | 4             | 0~2147483647  | 单位 ms                                                              |
| LED_R                | 1             | 0~255         | RGB 百分比                                                           |
| LED_G                | 1             | 0~255         |                                                                      |
| LED_B                | 1             | 0~255         |                                                                      |

---

#### 2.3.1.7 蜂鸣器控制（0x06）
方向：网关 → Badge/Hub

| 字段                 | 长度（字节） | 取值          | 说明                                                                 |
|----------------------|---------------|---------------|----------------------------------------------------------------------|
| 分组ID               | 1             | 0~255         | 目标角色                                                             |
| 模式开关             | 1             | 0x00~0xFF     | Bit0=蜂鸣模式(0常响/1闪烁)                                           |
| 开关                 | 1             | 0/1           | 模式0：0关1开；模式1忽略                                             |
| 蜂鸣开启时长         | 4             | 0~2147483647  | 单位 ms                                                              |
| 蜂鸣关闭时长         | 4             | 0~2147483647  | 单位 ms                                                              |
| 音量                 | 1             | 0~10          | 0最小，10最大                                                        |

---

#### 2.3.1.8 震动控制（0x07）
方向：网关 → Badge/Hub

| 字段                 | 长度（字节） | 取值          | 说明                                                                 |
|----------------------|---------------|---------------|----------------------------------------------------------------------|
| 分组ID               | 1             | 0~255         | 目标角色                                                             |
| 模式开关             | 1             | 0x00~0xFF     | Bit0=震动模式(0常震/1间歇)                                           |
| 开关                 | 1             | 0/1           | 模式0：0关1开；模式1忽略                                             |
| 动作类型             | 1             | 0/1           | 0=动作；1=设置                                                      |
| 震动开启时长         | 4             | 0~2147483647  | 单位 ms                                                              |
| 震动关闭时长         | 4             | 0~2147483647  | 单位 ms                                                              |

---

#### 2.3.1.9 LCD 内容控制（0x08）
方向：网关 → Badge/Hub

| 字段                 | 长度（字节） | 取值           | 说明                     |
|----------------------|---------------|----------------|--------------------------|
| 分组ID               | 1             | 0~255          | 目标角色                 |
| 第一行内容           | 20            | U8 str[20]     | 第一行显示文字           |
| 第二行内容           | 20            | U8 str[20]     | 第二行显示文字           |

---

##### 批注
- 另外请记得：对于长按（3秒），LCD 需要先显示一个确认消息，
  要求用户再次按下相同的报警按钮后，报警才会发送。若用户在4秒内未再次按下，则此次报警无效。
  对于短按（少于2秒），点击任意按钮时，LCD 应显示 badge 的电池状态。
  另外，我们也希望 LCD 可以显示从 dashboard 发送过来的简短文本消息。
  对于按键组合功能，例如重置 badge，需要同时按下两个按钮并持续 5 秒。
- 好的，这是设备的定位逻辑。它将会被实现。

#### 2.3.1.10 LCD 第二行开关（0x09）
方向：网关 → Badge/Hub
指令：云启动通知

| 字段         | 长度（字节） | 取值    | 说明                     |
|--------------|---------------|---------|--------------------------|
| 分组ID       | 1             | 0~255   | 目标角色  Bit0: admin |
||||Bit1：Nurses
||||Bit2：secure
||||Bit3：principal
||||Bit4：......xxx
||||Bit7:  all role               |
| 第二行使能   | 1             | 0/1     | 0关闭；1开启             |

#### 批注
- 这意味着什么？您能否确认一下，在我们的情况下，这两条线路默认都会处于启用状态？
- 这是液晶显示屏第二行内容的切换。
  根据“固件要求及 API V7 - SIE 更新 - 2026 年 2 月.xlsx”文件，液晶显示屏第二行的上下文可以选择启用或禁用。
  这在“Firmware Requirements & APIs V7 - SIE Update - Feb 2026.xlsx”文件的第 27 行中有规定。
- 明白了。这在 Wirepas 的系统中或许适用。最好默认启用这两条线路。请确认一下。
  另外，要实现一个尺寸稍大但仍能适应徽章式外（包括印刷电路板和外壳）的液晶显示屏需要做些什么呢？
- 1. 我们将将其设置为默认状态。
  2. 关于液晶显示器的结构，本文件中不应对此进行讨论。

---



#### 2.3.1.11 清除包（0x0A）
方向：网关 → Badge/Hub

| 字段       | 长度（字节） | 取值    | 说明                                                                 |
|------------|---------------|---------|----------------------------------------------------------------------|
| 分组ID     | 1             | 0~255   | 目标角色                                                             |
| 清除类型   | 1             | 0/1     | 0=清除所有状态（关灯）；1=全部解除（绿灯闪烁）                       |
##### 批注
- 请记得这里有两个明确不同的状态：
1.All Clear（全部安全）—— Badge 和 Hub 的 LED 会闪烁绿色。
2.Clear All Statuses（清除所有状态）—— 警报被完全清除，所有 Badge 和 Hub 的 LED 都会关闭，系统恢复到“正常”状态。
- OK. We change the specification
---


#### 2.3.1.12 设置分组ID（0x50）
方向：网关 → Badge/Hub

| 字段     | 长度（字节） | 取值    | 说明                                                                 |
|----------|---------------|---------|----------------------------------------------------------------------|
| 分组ID   | 1             | 0~255   | 设置设备角色<br/>Bit0=管理员；Bit1=护士；Bit2=安保；Bit3=校长；Bit7=保留 |
##### 批注
- 我们理解这里就是定义 multicast 和 broadcast groups 的地方，对吗？
- 是的，它适用于多播和广播组。如果该设备设置为 255，它将执行所有下行指令，而无需与组 ID 相匹配。
- 很好。正如在第 2.3.1.6 节中所述，我们同样需要弄清楚这些组是如何被定义和分配的。
- 此组标识符用于应用层。请查看 2.3.1.6 的响应信息。
---

## 3 CRC16
CRC 校验采用 CRC16/XMODEM 算法，C 语言实现如下：

```c
uint16_t xmodem_val;

void crc16_xmodem_init( void )
{
    xmodem_val = 0;
}

void crc16_xmodem_append( uint8_t *pbuf, uint16_t length )
{
    while( length-- ) {
        xmodem_val ^= (uint16_t)(*pbuf++) << 8;
        for( uint8_t i = 0; i < 8; ++i )
        {
            if( xmodem_val & 0x8000 ) {
                xmodem_val = (xmodem_val << 1) ^ 0x1021;
            } else {
                xmodem_val <<= 1;
            }
        }
    }
}

uint16_t crc16_xmodem_end( void )
{
    return xmodem_val;
}
```
### 使用说明
计算 CRC 前必须先调用 crc16_xmodem_init()

```c
uint8_t crc_data[] = {
0xAA, 0x55, 0x01, 0x00, 0x00, 0x08, 0x00, 0x03
};
crc16_xmodem_init();
crc16_xmodem_append( crc_data, sizeof(crc_data) );
LOG( "crc16_xmodem value1: %X\r\n", crc16_xmodem_end() );
crc16_xmodem_append( crc_data, sizeof(crc_data) );
LOG( "crc16_xmodem value2: %X\r\n", crc16_xmodem_end() );
```
运行结果
- crc16_xmodem value1: 58C7
- crc16_xmodem value2: E228

### 批注
- 除此之外，你们也会加入 frame counter（FCnt）来防止 replay attack（重放攻击），对吗？
- Lorawan协议能够抵御重放攻击。对于私有协议来说这无关紧要。它仅仅用于检查命令的完整性。
- LoRaWAN 协议是如何防止重放攻击的？这对于防止外来设备模仿我们的标识卡或集线器至关重要。
- LoRaWAN 防止重放攻击的核心安全机制由以下三个关键组件构成：1. 消息完整性码验证2. 帧计数器3. DevNonce/JoinNonce
  关于细节方面，请查阅“LoRaWAN 规范”

## LED 颜色配置
平台需支持直接下发 RGB 值，以满足标准响应协议（SRP）需求。以下颜色由后台（Dashboard）下发，不由胸牌触发：
| Command  | Trigger Source | Broadcast Target   | LED Color | Badge LCD Message |
|----------|----------------|---------------------|-----------|-------------------|
| Hold     | Dashboard only | All Badges & Hubs   | Purple    | Hold Alert        |
| Secure   | Dashboard only | All Badges & Hubs   | Blue      | Secure Alert      |
| Evacuate | Dashboard only | All Badges & Hubs   | Green     | Evacuate Alert    |
| Shelter  | Dashboard only | All Badges & Hubs   | Orange    | Shelter Alert     |

## 建议 RGB 配置
| Alert Type          | Color  | RGB         | Visibility Reason                                   |
|---------------------|--------|-------------|-----------------------------------------------------|
| Lockdown / Code Red | Red    | 255, 0, 0   | Maximum urgency, universally recognized             |
| Medical / Secure    | Blue   | 0, 100, 255 | Distinct from red and visible through diffusers    |
| Admin Assist        | Yellow | 255, 220, 0 | Very bright, easily seen in daylight               |
| All Clear / Evacuate| Green  | 0, 255, 80  | Bright but clearly different from yellow           |
| Hold                | Purple | 180, 0, 255 | Unique color not confused with others              |
| Shelter             | Orange | 255, 120, 0 | Strong color between red and yellow                |

## LoRaWAN 等级（A 或 B）
你选择 Class B，理由是 “胸牌与 Hub 需要接收服务器下行”。我们主要关注功耗问题，尤其是 Hub。你们是否已考虑？
### 批注
- 首先，我们必须为该用例选用类 B。
然后，要考虑类 B 的使用情况，包括周期、数据长度、速率和距离等因素。

## 离线降级机制
我们在评审系统设计时需要确认：网络完全中断时系统如何运行？
作为应急报警系统，必须保证：即使以太网、Wi‑Fi、LTE/4G 全部断开，校园内报警仍能正常工作。
请确认当前设计是否已覆盖以下场景：
1. 所有外网断开
2. 校园内有人按下胸牌紧急按键
我们的预期行为
1. 网关自动检测断网，自动进入本地降级模式（无需人工操作）
2. 切换到内置本地 LNS，继续处理 Badge/Hub 上行报警
3. 本地执行报警路由规则，下发正确下行：蜂鸣、LED、LCD 信息
4. 在局域网内提供精简版报警后台给现场人员
5. 本地缓存事件，联网后自动同步回 AWS 云端，保留原始时间戳用于审计
6. 网络恢复后自动切回主 AWS LNS

## 请确认当前设计是否已实现，或哪些点需要进一步讨论。
### 批注
- 网关目前只是一个转发器。如果需要考虑没有网络连接的情况，那么需要在本地部署 LNS。
  在我看来，网络的稳定性是由服务提供商来保证的。
- 看起来只要配备合适的处理器、内存、操作系统（OS）、存储设备以及嵌入式 ChirpStack v4 芯片，就能实现这一功能。
  烦请您进行检查并给出建议。